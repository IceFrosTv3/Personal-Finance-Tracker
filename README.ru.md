# Lumincash — Личный финансовый трекер

Одностраничное приложение для учёта личных доходов и расходов. Позволяет управлять категориями, просматривать транзакции и анализировать бюджет через графики.

> [English documentation](./README.md)

---

## Стек технологий

- **HTML5** — разметка шаблонов
- **SCSS** — стилизация (с разбивкой на партиалы)
- **JavaScript (ES6+)** — логика приложения, роутинг, компоненты
- **Vite** — сборщик
- **npm** — менеджер пакетов
- **Bootstrap 5** + **Bootstrap Icons** — UI-компоненты и иконки
- **Chart.js** — круговые графики доходов и расходов
- **Tempus Dominus** — датапикер

---

## Установка и запуск

```bash
# Клонировать репозиторий
git clone https://github.com/IceFrosTv2/Personal-Finance-Tracker.git
cd frontend

# Установить зависимости
npm install
```

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
VITE_HOST="http://localhost:3000"
```

`VITE_HOST` — базовый URL сервера бэкенда.

```bash
# Запустить в режиме разработки
npm run dev

# Собрать для продакшена
npm run build
```

---

## Структура проекта

```
frontend/
├── public/
│   └── templates/
│       ├── layout.html                     # Основной layout с сайдбаром
│       └── pages/
│           ├── auth/
│           │   ├── login.html
│           │   └── register.html
│           ├── dashboard.html
│           ├── 404.html
│           └── operations/
│               └── operations-list.html
├── src/
│   ├── main.js                             # Точка входа
│   ├── router.js                           # Клиентский роутер
│   ├── config/
│   │   └── config.js                       # Базовый URL API и имена маршрутов
│   ├── services/
│   │   ├── auth-service.js                 # Авторизация: вход, регистрация, выход
│   │   ├── categories-service.js           # CRUD категорий
│   │   └── operations-service.js           # CRUD операций и фильтрация
│   ├── components/
│   │   ├── layout.js                       # Сайдбар, баланс, состояние меню
│   │   └── pages/
│   │       ├── auth/
│   │       │   ├── login.js
│   │       │   └── register.js
│   │       ├── dashboard.js                # Графики с фильтрацией по периоду
│   │       ├── base-filters-page.js        # Базовый класс: логика фильтра периода
│   │       ├── operations/
│   │       │   ├── operations-list.js      # Список операций с фильтрами
│   │       │   └── operations-form.js      # Создание и редактирование операции
│   │       └── base-categories/
│   │           ├── base-categories.js      # Список категорий (доходы и расходы)
│   │           ├── base-categories-create.js
│   │           └── base-categories-edit.js
│   ├── utils/
│   │   ├── tokens-utils.js                 # Хранение токенов и логика обновления
│   │   ├── http-utils.js                   # HTTP-обёртка с заголовками авторизации
│   │   ├── validator-form.js               # Валидация форм
│   │   ├── toast-utils.js                  # Уведомления об ошибках
│   │   ├── modal-utils.js                  # Модальное окно подтверждения удаления
│   │   ├── date-picker.js                  # Обёртка над Tempus Dominus
│   │   └── select-period.js                # Состояние кнопок фильтра периода
│   └── styles/
│       ├── styles.scss                     # Точка входа стилей
│       ├── _auth.scss
│       ├── _layout.scss
│       ├── _dashboard.scss
│       └── _operations.scss
└── index.html
```

---

## Функциональность

- Регистрация и авторизация с клиентской валидацией форм
- Дашборд с балансом и круговыми графиками доходов и расходов
- Фильтрация по периоду: сегодня / неделя / месяц / год / всё время / произвольный диапазон
- Управление категориями доходов и расходов (создание, редактирование, удаление с подтверждением)
- Список транзакций с теми же фильтрами по периоду
- Создание и редактирование операций: тип, категория, сумма, дата, комментарий
- Баланс обновляется автоматически после любого действия с операциями
- Операции с удалёнными категориями отображаются как «без категории» на графиках
- Клиентский роутинг без перезагрузки страницы
- Адаптивная вёрстка: сворачиваемый сайдбар на десктопе, выдвижное меню на мобильных устройствах (< 650px)
- Тёмная тема (переключается кнопкой в сайдбаре, выбор сохраняется в localStorage)
- Постраничная навигация в списке транзакций (настраиваемый размер страницы, кнопки Назад / Вперёд)

---

## Лицензия

[MIT](./LICENSE)

---

## Автор

**Frontend Developer** — Andrei
GitHub: [@IceFrosTv2](https://github.com/icefrostv2)
